sudo: required
services:
  - docker

before_install:
  - docker build -t ajitmathew82/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run ajitmathew82/react-test npm test -- --coverage

after_success:
  - docker build -t ajitmathew82/multi-client ./client
  - docker build -t ajitmathew82/multi-nginx ./nginx
  - docker build -t ajitmathew82/multi-server ./server
  - docker build -t ajitmathew82/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push ajitmathew82/multi-client
  - docker push ajitmathew82/multi-nginx
  - docker push ajitmathew82/multi-server
  - docker push ajitmathew82/multi-worker
